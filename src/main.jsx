import React from "react";
import ReactDOM from "react-dom/client";
import MLBEdge from "./MLBEdge.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MLBEdge />
  </React.StrictMode>
);
